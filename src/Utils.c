#include "Utils.h"
int maxi(int a, int b)
{
    return a > b ? a : b;
}
int maxd(double a, double b)
{
    return a > b ? a : b;
}

int mini(int a, int b)
{
    return a < b ? a : b;
}
int mind(double a, double b)
{
    return a < b ? a : b;
}